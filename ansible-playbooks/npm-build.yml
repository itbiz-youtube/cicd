---
- name:
  hosts: all
  become: true

  tasks:
    - name: Show npm version
      shell: "npm -v"

    - name: delete current folder
      shell: "rm -rf /home/zebra/radiochiptune"

    - name: Clone a repo 
      git:
        repo: https://github.com/zebra48r/radiochiptune.git
        dest: /home/zebra/radiochiptune
    
    - name: Update npm
      npm:
        path=/home/zebra/radiochiptune

    - name: make build
      shell: "cd /home/zebra/radiochiptune && npm build"
      register: npm_build

    - name: debug message npm build
      debug: npm_build.stdout_lines

    - name: make build and run server 
      shell: "cd /home/zebra/radiochiptune && serve -s build -l 4000"